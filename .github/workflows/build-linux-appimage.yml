name: Build Linux Packages

on:
  push:
    branches:
      - main
    paths:
      - 'linux_gui/src/**'
      - 'linux_gui/assets/**'
      - 'linux_gui/requirements.txt'
      - 'linux_gui/build_appimage.sh'
      - 'linux_gui/build_deb.sh'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl file fuse libfuse2 dpkg-dev

      - name: Build AppImage
        working-directory: linux_gui
        run: |
          chmod +x build_appimage.sh
          ./build_appimage.sh

      - name: Build .deb package
        working-directory: linux_gui
        run: |
          chmod +x build_deb.sh
          ./build_deb.sh

      - name: Move packages to dist
        run: |
          mkdir -p linux_gui/dist
          rm -f linux_gui/dist/*.AppImage linux_gui/dist/*.deb
          mv linux_gui/build/*.AppImage linux_gui/dist/
          mv linux_gui/build/*.deb linux_gui/dist/

      - name: Commit packages to repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add linux_gui/dist/*.AppImage linux_gui/dist/*.deb
          git diff --staged --quiet || git commit -m "build: update Linux packages [skip ci]"
          git push
